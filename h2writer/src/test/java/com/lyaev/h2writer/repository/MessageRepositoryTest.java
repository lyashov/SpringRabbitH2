package com.lyaev.h2writer.repository;

import com.lyaev.h2writer.model.MessageEntity;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;


@RunWith(SpringRunner.class)
@SpringBootTest
public class MessageRepositoryTest {
   // @Autowired
   // private MessageService messageService;

    @Test
    public void doTest() {
        // given
       // MessageEntity mes = new MessageEntity("test message");

      //  messageService.saveMessage(mes.getMessage());

        // when
      //  MessageEntity found = messageService.findByMessage(mes.getMessage());

        // then

       // assertThat(found.getMessage()).isEqualTo(mes.getMessage());
    }
}